from flask import render_template, request, redirect	# any other flask imports needed
from config import app, db	# here's where we're importing the variables from config!
# some Model classes here
# routes
# app.run